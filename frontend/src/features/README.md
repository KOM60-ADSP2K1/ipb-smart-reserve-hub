# Features

Feature modules own domain workflows such as auth, facilities, reservations, notifications, staff, and admin. Keep backend integration behind typed API/query helpers instead of calling `fetch` directly from page components.
