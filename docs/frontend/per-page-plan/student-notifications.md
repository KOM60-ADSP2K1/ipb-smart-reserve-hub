# Student Notifications

- Route: `/student/notifications`
- Role/access: student.
- Backend APIs: `GET /notifications`, `POST /notifications/{notification_id}/read`.
- Core data: notification message, read state, created time, related entity if exposed.
- Mutations/actions: mark notification read.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide whether notification links need typed target metadata.
