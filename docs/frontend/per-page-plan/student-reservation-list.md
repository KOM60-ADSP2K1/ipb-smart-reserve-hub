# Student Reservation List

- Route: `/student/reservations`
- Role/access: student.
- Backend APIs: `GET /student/reservations`.
- Core data: reservation code, status, facility, schedule, deadlines, price, review summary.
- Mutations/actions: navigate to detail; possible cancel action if supported by status.
- Design references: `10 Reservation Page - On Going.png`, `11 Reservation Page - History.png`
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: list endpoint does not expose explicit pagination or server-side status filters.
