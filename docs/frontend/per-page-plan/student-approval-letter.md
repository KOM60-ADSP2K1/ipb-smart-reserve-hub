# Student Approval Letter

- Route: `/student/reservations/:reservationId/approval-letter`
- Role/access: student.
- Backend APIs: `GET /student/reservations/{reservation_id}/approval-letter`, `GET /student/reservations/{reservation_id}/approval-letter/download`, `POST /student/reservations/{reservation_id}/signed-approval-letter`.
- Core data: generated approval letter metadata, download file, signed letter upload metadata.
- Mutations/actions: download approval letter, upload signed approval letter.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: none known.
