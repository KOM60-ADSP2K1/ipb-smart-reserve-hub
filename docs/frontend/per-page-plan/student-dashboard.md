# Student Dashboard

- Route: `/student`
- Role/access: student.
- Backend APIs: `GET /student/shell`, `GET /student/reservations`, `GET /notifications`.
- Core data: student identity, upcoming reservations, recent notifications.
- Mutations/actions: mark notification as read.
- Design references: `01 Home Page.png`
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide whether `/student` is a true dashboard or redirects to `/student/facilities` initially.
