# Facility Detail

- Route: `/student/facilities/:facilityId`
- Role/access: student.
- Backend APIs: `GET /facilities/{facility_id}`, optional `GET /facilities/{facility_id}/calendar`.
- Core data: facility profile, images, open hours, price, contact, calendar entries.
- Mutations/actions: start reservation flow.
- Design references: `02 Facility Details Page.png`
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide whether public calendar is needed on first detail implementation or only during time selection.
