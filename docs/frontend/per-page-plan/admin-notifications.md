# Admin Notifications

- Route: `/admin/notifications`
- Role/access: admin.
- Backend APIs: `GET /notifications`, `POST /notifications/{notification_id}/read`.
- Core data: admin notifications.
- Mutations/actions: mark notification read.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide whether notification target metadata is sufficient for routing to admin pages.
