# Admin Reviews

- Route: `/admin/reviews`
- Role/access: admin.
- Backend APIs: `GET /admin/reviews`, `POST /admin/reviews/{review_id}/delete`, `POST /admin/reviews/{review_id}/restore`.
- Core data: review list, deletion state, filter fields.
- Mutations/actions: remove review, restore review.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: none known.
