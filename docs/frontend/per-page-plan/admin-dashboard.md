# Admin Dashboard

- Route: `/admin`
- Role/access: admin.
- Backend APIs: `GET /admin/shell`, `GET /admin/system-status`, `GET /admin/audit-logs`, `GET /notifications`.
- Core data: admin identity, system status, recent audit activity, notifications.
- Mutations/actions: mark notification read.
- Design references: `20 Admin Landing Page.png`
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide dashboard metrics; no dedicated admin dashboard aggregate endpoint exists.
