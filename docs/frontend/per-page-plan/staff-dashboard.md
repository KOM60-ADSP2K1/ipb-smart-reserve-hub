# Staff Dashboard

- Route: `/staff`
- Role/access: staff.
- Backend APIs: `GET /staff/shell`, `GET /staff/facilities`, `GET /notifications`.
- Core data: staff identity, assigned facilities, notification summary.
- Mutations/actions: mark notification read.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: decide whether dashboard needs reservation review counts; no aggregate review queue endpoint is currently exposed.
