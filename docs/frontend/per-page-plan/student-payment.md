# Student Payment

- Route: `/student/reservations/:reservationId/payment`
- Role/access: student.
- Backend APIs: `GET /student/reservations/{reservation_id}/payment`, `POST /student/reservations/{reservation_id}/payment-receipt`.
- Core data: amount, payment instructions, uploaded receipt metadata.
- Mutations/actions: upload payment receipt.
- Design references: none selected yet.
- Design documentation:
  - Primary user goal:
  - Layout decisions:
  - State decisions:
  - Responsive decisions:
  - Visual test scenarios:
- Open questions / backend gaps: none known.
